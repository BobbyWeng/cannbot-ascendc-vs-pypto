operator_name: or
formula: "Y = LogicalOr(X1, X2)"
category: element-wise logical
inputs:
  - name: x1
    shape: "[B, 256, 384]"
    dtype: bool
    layout: ND contiguous
  - name: x2
    shape: "[B, 256, 384]"
    dtype: bool
    layout: ND contiguous
outputs:
  - name: y
    shape: "[B, 256, 384]"
    dtype: bool
    layout: ND contiguous
batches: [1, 2, 4, 8, 16, 32, 64]
precision:
  rtol: 0
  atol: 0
  require_bitwise: true
  note: "BOOL OR must be bitwise identical. BOOL is stored as uint8 (0=False, 1=True). Any non-zero input treated as True."
seed: 20260715
