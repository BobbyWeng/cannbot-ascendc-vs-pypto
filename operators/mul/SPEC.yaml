operator_name: mul
formula: "Y = X1 * X2"
category: element-wise arithmetic
inputs:
  - name: x1
    shape: "[B, 3, 4, 256, 32]"
    dtype: float16
    layout: ND contiguous
  - name: x2
    shape: "[B, 3, 4, 256, 32]"
    dtype: float16
    layout: ND contiguous
outputs:
  - name: y
    shape: "[B, 3, 4, 256, 32]"
    dtype: float16
    layout: ND contiguous
batches: [1, 2, 4, 8, 16, 32, 64]
precision:
  rtol: 0
  atol: 0
  require_bitwise: true
  note: "FP16 Mul must match IEEE 754 semantics. Signed-zero must NOT be exempted (unlike ReLU). +0 × -0 = -0 per IEEE."
seed: 20260715
