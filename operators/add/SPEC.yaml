operator_name: add
formula: "Y = ((X1 + X2) + X3) + X4"
category: element-wise arithmetic
inputs:
  - name: x1
    shape: "[B, 256, 384]"
    dtype: float16
    layout: ND contiguous
  - name: x2
    shape: "[B, 256, 384]"
    dtype: float16
    layout: ND contiguous
  - name: x3
    shape: "[B, 256, 384]"
    dtype: float16
    layout: ND contiguous
  - name: x4
    shape: "[B, 256, 384]"
    dtype: float16
    layout: ND contiguous
outputs:
  - name: y
    shape: "[B, 256, 384]"
    dtype: float16
    layout: ND contiguous
batches: [1, 2, 4, 8, 16, 32, 64]
precision:
  rtol: 0
  atol: 0
  require_bitwise: true
  note: "FP16 Add must be bitwise identical to fixed left-associative order ((X1+X2)+X3)+X4. Signed-zero is handled per IEEE 754."
seed: 20260715
