#include <cstdint>
#include <iostream>
#include <vector>
#include <string>
#include <chrono>
#include <algorithm>
#include <cmath>
#include <cstring>
#include "acl/acl.h"
#include "expand_tiling.h"
#include "data_utils.h"
#include "expand_kernel.asc"

struct BenchResult {
    double median_us;
    double mean_us;
    double min_us;
    double p90_us;
    double std_us;
    std::vector<double> latencies_us;
};

BenchResult measure_kernel_only(uint32_t blockNum, aclrtStream stream,
    void *inputDevice, void *outputDevice, void *tilingDevice,
    int warmup, int loops, int repeat)
{
    for (int i = 0; i < warmup; i++) {
        expand_kernel<<<blockNum, nullptr, stream>>>((GM_ADDR)inputDevice, (GM_ADDR)outputDevice, (GM_ADDR)tilingDevice);
    }
    aclrtSynchronizeStream(stream);

    aclrtEvent start, stop;
    aclrtCreateEvent(&start);
    aclrtCreateEvent(&stop);

    std::vector<double> latencies;
    latencies.reserve(repeat);

    for (int r = 0; r < repeat; r++) {
        aclrtRecordEvent(start, stream);
        for (int i = 0; i < loops; i++) {
            expand_kernel<<<blockNum, nullptr, stream>>>((GM_ADDR)inputDevice, (GM_ADDR)outputDevice, (GM_ADDR)tilingDevice);
        }
        aclrtRecordEvent(stop, stream);
        aclrtSynchronizeStream(stream);

        float elapsed_ms = 0.0f;
        aclrtEventElapsedTime(&elapsed_ms, start, stop);
        double elapsed_us = elapsed_ms * 1000.0 / loops;
        latencies.push_back(elapsed_us);
    }

    aclrtDestroyEvent(start);
    aclrtDestroyEvent(stop);

    std::sort(latencies.begin(), latencies.end());
    BenchResult res;
    res.latencies_us = latencies;
    res.min_us = latencies.front();
    res.median_us = latencies[latencies.size() / 2];
    res.p90_us = latencies[(size_t)(latencies.size() * 0.9)];

    double sum = 0;
    for (auto v : latencies) sum += v;
    res.mean_us = sum / latencies.size();

    double sq_sum = 0;
    for (auto v : latencies) sq_sum += (v - res.mean_us) * (v - res.mean_us);
    res.std_us = std::sqrt(sq_sum / latencies.size());

    return res;
}

int run_batch(uint32_t batch, uint32_t blockDim, uint32_t tileLen,
    int32_t deviceId, int warmup, int loops, int repeat,
    const std::string &dataDir, const std::string &outputDir)
{
    uint64_t totalOutputElements = (uint64_t)batch * 256 * 384;
    uint64_t totalInputElements = (uint64_t)batch * 256 * 1;
    size_t outputByteSize = totalOutputElements * sizeof(half);
    size_t inputByteSize = totalInputElements * sizeof(half);

    std::string inputPath = dataDir + "/input_b" + std::to_string(batch) + "_fp16.bin";
    std::string outputPath = outputDir + "/output_b" + std::to_string(batch) + ".bin";

    uint32_t blockNum = blockDim;
    uint32_t inputRowElems = 256;
    uint32_t outputRowElems = 256 * 384;
    uint32_t expandFactor = 384;

    uint64_t totalTiles = (totalOutputElements + tileLen - 1) / tileLen;
    if (blockNum > totalTiles) blockNum = (uint32_t)totalTiles;
    if (blockNum == 0) blockNum = 1;
    uint64_t tilesPerCore = (totalTiles + blockNum - 1) / blockNum;
    uint64_t numPerCore = tilesPerCore * tileLen;
    uint64_t tailNumLastCore = totalOutputElements - numPerCore * (blockNum - 1);
    if ((int64_t)tailNumLastCore <= 0 || tailNumLastCore > numPerCore) {
        tailNumLastCore = totalOutputElements - numPerCore * (blockNum - 1);
    }
    while ((int64_t)tailNumLastCore <= 0 && blockNum > 1) {
        blockNum--;
        tilesPerCore = (totalTiles + blockNum - 1) / blockNum;
        numPerCore = tilesPerCore * tileLen;
        tailNumLastCore = totalOutputElements - numPerCore * (blockNum - 1);
    }

    ExpandTilingData tiling;
    tiling.blockNum = blockNum;
    tiling.totalInputElements = totalInputElements;
    tiling.totalOutputElements = totalOutputElements;
    tiling.numPerCore = numPerCore;
    tiling.tailNumLastCore = tailNumLastCore;
    tiling.inputRowElems = inputRowElems;
    tiling.outputRowElems = outputRowElems;
    tiling.expandFactor = expandFactor;

    uint8_t *tilingDevice = nullptr;
    aclrtMalloc((void **)&tilingDevice, sizeof(ExpandTilingData), ACL_MEM_MALLOC_HUGE_FIRST);
    aclrtMemcpy(tilingDevice, sizeof(ExpandTilingData), &tiling,
        sizeof(ExpandTilingData), ACL_MEMCPY_HOST_TO_DEVICE);

    void *inputDevice = nullptr;
    void *outputDevice = nullptr;
    aclrtMalloc(&inputDevice, inputByteSize, ACL_MEM_MALLOC_HUGE_FIRST);
    aclrtMalloc(&outputDevice, outputByteSize, ACL_MEM_MALLOC_HUGE_FIRST);

    uint8_t *hostBuf = new uint8_t[inputByteSize];
    ReadFile(inputPath, inputByteSize, hostBuf, inputByteSize);
    aclrtMemcpy(inputDevice, inputByteSize, hostBuf, inputByteSize, ACL_MEMCPY_HOST_TO_DEVICE);
    delete[] hostBuf;

    aclrtStream stream;
    aclrtCreateStream(&stream);

    BenchResult res = measure_kernel_only(blockNum, stream,
        inputDevice, outputDevice, tilingDevice,
        warmup, loops, repeat);

    hostBuf = new uint8_t[outputByteSize];
    aclrtMemcpy(hostBuf, outputByteSize, outputDevice, outputByteSize, ACL_MEMCPY_DEVICE_TO_HOST);
    WriteFile(outputPath, hostBuf, outputByteSize);
    delete[] hostBuf;

    aclrtDestroyStream(stream);
    aclrtFree(inputDevice);
    aclrtFree(outputDevice);
    aclrtFree(tilingDevice);

    double inputBytes = (double)inputByteSize;
    double outputBytes = (double)outputByteSize;
    double totalBytes = inputBytes + outputBytes;
    double bw = totalBytes / res.median_us / 1e3;

    printf("RESULT batch=%u blockNum=%u tileLen=%u median_us=%.1f mean_us=%.1f min_us=%.1f p90_us=%.1f std_us=%.2f cv=%.2f bw=%.3f\n",
        batch, blockNum, tileLen,
        res.median_us, res.mean_us, res.min_us, res.p90_us, res.std_us,
        res.std_us / res.mean_us * 100.0, bw);
    printf("RAW_LATENCIES:");
    for (size_t i = 0; i < res.latencies_us.size(); i++) {
        if (i > 0) printf(",");
        printf("%.1f", res.latencies_us[i]);
    }
    printf("\n");

    return 0;
}

int32_t main(int32_t argc, char *argv[])
{
    aclInit(nullptr);
    int32_t deviceId = 0;
    if (argc > 1) deviceId = std::atoi(argv[1]);

    auto ret = aclrtSetDevice(deviceId);
    if (ret != ACL_SUCCESS) {
        printf("aclrtSetDevice(%d) failed, ret=%d\n", deviceId, ret);
        aclFinalize();
        return ret;
    }

    uint32_t batch = 1;
    uint32_t blockDim = 20;
    uint32_t tileLen = 8192;
    int warmup = 100;
    int loops = 1000;
    int repeat = 10;
    std::string dataDir = "operators/expand/data";
    std::string outputDir = "operators/expand/ascendc/build/output";

    if (argc > 2) batch = std::atoi(argv[2]);
    if (argc > 3) blockDim = std::atoi(argv[3]);
    if (argc > 4) tileLen = std::atoi(argv[4]);
    if (argc > 5) warmup = std::atoi(argv[5]);
    if (argc > 6) loops = std::atoi(argv[6]);
    if (argc > 7) repeat = std::atoi(argv[7]);
    if (argc > 8) dataDir = argv[8];
    if (argc > 9) outputDir = argv[9];

    run_batch(batch, blockDim, tileLen, deviceId, warmup, loops, repeat, dataDir, outputDir);

    aclrtResetDevice(deviceId);
    aclFinalize();
    return 0;
}
